# Fallback config. Override the profile at launch, e.g.
#   java -jar barberapp.jar --spring.profiles.active=prod
spring.profiles.active=dev
spring.application.name=barberapp
server.port=8080

security.bcrypt.strength=12
